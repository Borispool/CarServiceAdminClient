<Window x:Class="CarServiceAdminClient.AddClientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:CarServiceAdminClient.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="420" Width="450"
        WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" ResizeMode="NoResize"
        Background="#F4F7F6">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title, FallbackValue='Заголовок'}" Style="{StaticResource HeaderStyle}"/>

        <ScrollViewer Grid.Row="1" VerticalAlignment="Center" VerticalScrollBarVisibility="Auto" Padding="0,0,15,0">
            <StackPanel>
                <TextBlock Text="Ім'я*" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,5"/>
                <!-- ВИПРАВЛЕННЯ: Додано UpdateSourceTrigger -->
                <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                <TextBlock Text="Прізвище*" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,5"/>
                <!-- ВИПРАВЛЕННЯ: Додано UpdateSourceTrigger -->
                <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                <TextBlock Text="Телефон*" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,5"/>
                <!-- ВИПРАВЛЕННЯ: Додано UpdateSourceTrigger -->
                <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                <TextBlock Text="Email" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,5"/>
                <!-- ВИПРАВЛЕННЯ: Додано UpdateSourceTrigger -->
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Зберегти" IsDefault="True" Click="SaveButton_Click"/>
            <Button Content="Скасувати" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>