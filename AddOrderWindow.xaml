<Window x:Class="CarServiceAdminClient.AddOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Нове Замовлення" Height="350" Width="450"
        WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" ResizeMode="NoResize"
        Background="#F4F7F6">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Нове Замовлення" Style="{StaticResource HeaderStyle}"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock Text="Опис проблеми*" Style="{StaticResource LabelTextStyle}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20" Height="80" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

            <CheckBox IsChecked="{Binding IsRecurringProblem}" 
                      Content="Ця проблема вже виникала раніше" 
                      VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Зберегти" IsDefault="True" Click="SaveButton_Click"/>
            <Button Content="Скасувати" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>